version: '3.8'

services:
  oksana-security:
    build: .
    container_name: oksana-security-site
    ports:
      - "3000:8080"
    restart: unless-stopped
    environment:
      - NGINX_PORT=8080
    # volumes:
      # Optional: Mount for live development
      # - ./:/usr/share/nginx/html:ro
    labels:
      - "com.docker.compose.project=oksana-security"

  # Optional: Add nginx proxy for custom domain
  # nginx-proxy:
  #   image: nginx:alpine
  #   container_name: oksana-security-proxy
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - oksana-security